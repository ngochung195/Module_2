Prompt 1: Tại sao khi ghi bộ đệm xuống đĩa, tôi phải dùng hàm write(buffer, 0, bytesRead) thay vì write(buffer)? Nếu dùng write(buffer) thì file đích có bị dư thừa byte rác ở vòng lặp cuối cùng không?

Prompt 2: Làm thế nào để sinh ra một file dummy.bin dung lượng khoảng 50MB hoàn toàn bằng code Java phục vụ cho việc test I/O?