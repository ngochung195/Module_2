Prompt 1: Việc đặt từ khóa synchronized cho toàn bộ một hàm (method) so với việc chỉ đặt cho một khối lệnh (block) bên trong hàm đó sẽ ảnh hưởng thế nào đến hiệu năng của hệ thống có hàng triệu truy cập?

Prompt 2: Dưới đây là cách tôi áp dụng Lock Ordering để giải quyết Deadlock. Cấu trúc synchronized lồng nhau của tôi đã đúng chuẩn chưa?